# add-claycore-v1

Pure C++20 SDF + voxel engine library: single-source kernels, scene/tape evaluation, brick cache, CPU/Metal/CUDA/OpenCL backends, meshing, file I/O, Python bindings, C ABI for Swift
