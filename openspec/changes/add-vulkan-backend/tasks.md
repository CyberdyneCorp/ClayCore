# Tasks: add-vulkan-backend

- [ ] 1.1 DECIDE and record in `design.md`: the shader route — clspv over the existing OpenCL amalgamation (no new dialect, source already gated), a GLSL port, or Slang/HLSL. Decide on a real compile of the current kernel set, not on reputation. Verify clspv against the C subset the shim macros already constrain, first, because that is what makes the cheap route plausible
- [ ] 1.2 DECIDE and record: minimum Vulkan version and the extension set, chosen from what target hardware actually has. Whether `VK_KHR_shader_float16_int8` is present decides whether `fp16_storage` is claimed
- [ ] 1.3 Backend skeleton behind the existing interface: registration, capability flags, `Unsupported` for raycast and for device meshing if not implemented
- [ ] 1.4 `eval_points` and the grid evaluation that fills bricks
- [ ] 1.5 Tape residency and buffer reuse from the start — this is a known cost on the Metal path and there is no reason to build it in again
- [ ] 1.6 CMake preset, off by default; a machine with no Vulkan SDK configures, builds and tests exactly as today
- [ ] 1.7 Shaders generated by the build from the kernel headers, never committed as generated output
- [ ] 1.8 Dialect-check profile if the chosen route needs one, wired into `check_kernel_dialect.py` so a kernel change that breaks it fails per-push without a device
- [ ] 1.9 Parity against the CPU reference on a REAL device, at the standard GPU tolerance, over every kernel and the composed golden scenes
- [ ] 1.10 CI job against a software runtime (lavapipe or SwiftShader) whose NAME and documentation say it gates plumbing — SPIR-V validity, descriptors, dispatch, readback — and not arithmetic. The removed OpenCL/pocl job is the precedent for what happens when this is left ambiguous
- [ ] 1.11 Add Vulkan device parity to `docs/RELEASE.md`'s manual hardware-dependent checks and to `tools/release_check.py`
- [ ] 1.12 Find the brick crossover for this backend rather than inheriting Metal's 16³, and record it
- [ ] 1.13 Update `docs/05-claycore-library.md`'s backend table: Vulkan's tier, what it implements, and its relationship to OpenCL — without deciding OpenCL's retirement here
