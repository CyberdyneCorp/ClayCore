# drag-a-layer-without-a-refill

A rigid layer transform states that the layer's surface is exactly the old one transformed, and a transform gesture pays one invalidation on drop instead of one per frame
