# Names in an in-flight change's tasks.md that do not resolve in the tree, and
# that predate `tools/check_task_symbols.py`.
#
# This file is DEBT, recorded rather than hidden. Each line is the change and
# the exact backticked span the gate could not resolve. Entries are added by
# hand only: a new change cannot acquire one by accident, which is the whole
# point of keeping the list here instead of loosening the matcher.
#
# Recorded 2026-09-06, when the gate was written. Nothing was edited in another
# change to produce this list — an owner clears their own rows by correcting the
# name or by building the thing.
#
# AUDITED WHEN THE GATE WAS WIRED INTO CI, and three rows came off it. All
# three were the same gate defect rather than debt anyone owed: `upload_tape`,
# `MetalBackend::upload_tape` and `wait_for` live in `backends/metal/`, and
# `backends/` was not one of the directories the gate searched. The audit
# itself only became possible once this file stopped being part of the tree the
# gate searches: it lives under `tools/`, so every name written here used to
# resolve by its own record. What is left below is debt in the two honest shapes — a name that
# belongs to another repository or to a build artefact, and a name a change has
# promised and not yet built.
#
# RE-AUDITED once the gate learned to read a qualified name whole. Until then it
# searched for the trailing identifier alone, so `MetalBackend::upload_tape` had
# been struck on evidence about `upload_tape` and nothing else — the class was
# never looked at. Every struck row and every row below was re-run against the
# corrected matcher: all three strikes hold (`upload_tape` and `wait_for` are
# both members of `class MetalBackend` in `backends/metal/metal_backend.cpp`),
# and every row below still fails to resolve, so none of them is a strike this
# gate was simply unable to see.
#
# A ROW IS RETIRED BY THE GATE, NOT BY GOODWILL. Every row here is re-read on
# every run: if the change stops citing the span, or the span starts resolving,
# the gate FAILS and names the row to delete. A debt list nobody retires stops
# describing the tree and goes on quietly exempting a name long after the debt
# is paid -- so if that name is later renamed away, the gate stays silent about
# the very thing it exists to catch. This sentence used to be advice to a human;
# `--self-test` is where both directions are proved.
#
# The live example: `LayerComposition` resolves on main (it landed with the
# layer-composition change) and does not resolve on the branch this file was
# audited from. Nobody has to remember to delete its row -- the first run after
# that merge fails and says so.
#
# <change>\t<span>

add-claycore-bridge	brush/procedural_mask.h
add-claycore-bridge	docs/sculpt-handoff-format.md
add-claycore-bridge	fieldSampledMaps
add-claycore-bridge	brush/surface_measure.h
add-device-perf-gates	Headers/clay/kernel/
add-device-perf-gates	dist/claycore.xcframework
add-mobile-thread-scheduling	../../backends/cpu/
correct-the-undo-scope	lattice_gizmo_preview
fold-the-layers-with-an-operator	LayerComposition
make-device-timing-position-independent	Shared/
speed-the-metal-path	bytesNoCopy
survive-a-crash	examples/60
